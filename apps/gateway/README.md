# gateway

API Gateway (JWT, RBAC, routage avance). Le socle HTTP Moleculer (`GET /health`) est implemente dans [`services/api`](../../services/api) (issue [#5](https://github.com/JeanBroche/A4_projet_web_avance/issues/5)). Ce package sera enrichi pour l'authentification et le routage metier dans une issue ulterieure.
