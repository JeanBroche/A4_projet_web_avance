# gateway

API Gateway (JWT, RBAC, routage avance). Ce package accueillera le socle HTTP Moleculer (`GET /health`, moleculer-web) et le routage metier (issue [#5](https://github.com/JeanBroche/A4_projet_web_avance/issues/5)).
