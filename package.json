{
  "name": "aeronexis-erp",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "lint": "pnpm -r run lint",
    "test": "pnpm -r run test",
    "build": "pnpm -r run build",
    "docker:up": "node -e \"console.log('Docker Compose sera disponible avec l issue #4'); process.exit(0)\"",
    "docker:down": "node -e \"console.log('Docker Compose sera disponible avec l issue #4'); process.exit(0)\""
  }
}